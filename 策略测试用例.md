# 自选策略 —— 测试用例

配套脚本：`tools/seed_strategies.py`（已为你播入 5 条示例策略）。

> 重新播种：`python tools/seed_strategies.py`
> 全部清空：`python tools/seed_strategies.py --clear`

---

## 已经帮你建好的 5 条策略

每条都覆盖了不同的参数类型，用来验证各类条件都能正确存储和执行。

| 策略名 | 条件 | 实测命中 |
|--------|------|---------|
| **放量上涨** | 股价 5~30 元 · 涨幅 2%~7% · 量比≥2 · 成交额≥5亿 · 按量比降序 | 20 只 |
| **均线多头** | 涨幅≥0 · 成交额≥3亿 · 创60日新高 · 均线多头 · 按涨幅降序 | 42 只 |
| **低吸企稳** | 股价 5~40 元 · 涨幅 -4%~0% · 成交额≥2亿 · 20日均振幅≤4% · 按成交额降序 | 356 只 |
| **连涨强势** | 涨幅≥0 · 连涨≥3天 · 成交额≥2亿 · 均线多头 · **限定创业板+深市主板** · 按连涨降序 | 29 只 |
| **创20日新高** | 振幅≤10% · 成交额≥3亿 · 创20日新高 · **限定沪市主板+深市主板** · 按成交额降序 | 46 只 |

> 命中数会随行情变化，重点是**条件是否被正确执行**（比如"连涨强势"里不该出现科创板股票）。
>
> 注：**低吸企稳**故意设得比较宽（命中 356 只），用来测"条件太宽时助手会怎么处理"。

---

## A. 面板功能测试

点左下角 **⚡ 自选策略**。

### A1 列表与搜索

| 操作 | 预期 |
|------|------|
| 打开面板 | 显示 5 个策略，每个带条件摘要和「试跑 / 编辑 / 删除」 |
| 顶部统计 | 「共 5 个策略 · 累计执行 N 次」 |
| 搜索框输入「放量」 | 只剩「放量上涨」 |
| 搜索框输入「量比」 | 出现「放量上涨」（**按条件文字也能搜到**） |
| 搜索框输入「均线」 | 出现「均线多头」和「连涨强势」（都含均线条件） |
| 搜索框输入「不存在的东西」 | 列表为空 |

### A2 试跑

点任意策略的「试跑」→ 下方出现结果表格（代码、名称、收盘、涨跌幅、量比…）。

**要特别核对**：点「连涨强势」试跑，结果里**不应该有 688 / 689 开头的科创板股票**，
因为这条件限定了创业板+深市主板。同理「创20日新高」不该出现创业板。

### A3 新建

点「新建策略」，填：

- 名称：`测试用-低价股`
- 描述：随便写
- 股价上限：`10`
- 创20日新高：勾选
- 排序字段：涨跌幅

点保存 → 列表变成 6 个，新策略出现在最上面，条件摘要显示
「股价 ≤ 10.0 元；创 20 日新高；按涨跌幅降序」。

### A4 编辑

点「测试用-低价股」的「编辑」→ 表单回填现有条件（**这里要看有没有回填正确**，
尤其是成交额这种带单位的字段不能变成奇怪的数字）→ 把股价上限改成 `20` → 保存。

预期：条件变慢成「股价 ≤ 20.0 元」，**不会新增一条**（同名视为更新）。

### A5 删除

删掉「测试用-低价股」→ 二次确认 → 列表回到 5 个。

### A6 边界

| 操作 | 预期 |
|------|------|
| 名称留空点保存 | 提示「请填写策略名称」 |
| 一个条件都不填点保存 | 提示「至少要设置一个筛选条件」 |
| 名称填 41 个字 | 输入框本身就限制到 40 字 |

---

## B. 对话中使用（重点）

### B1 按名字执行

输入：**用我的放量上涨策略选股**

预期：
- 出现「执行自选策略」卡片，摘要显示「策略『放量上涨』命中 N 只」
- 回复里**说清用的是哪个策略、包含哪些条件**，并给出股票列表
- 展开卡片能看到结果表格

**核对点**：复述的条件必须和面板里一致（5~30 元 / 2%~7% / 量比≥2 / 成交额≥5亿）。

### B2 模糊名字

输入：**用那个低吸的策略跑一下**

预期：助手能匹配到「低吸企稳」并执行。
（实现上先精确匹配、再模糊包含匹配）

### B3 不存在的名字

输入：**用我的涨停敢死队策略选股**

预期：明确说找不到这个策略，并**列出你实际有哪些策略**，而不是硬编一个结果。

### B4 问有哪些策略

输入：**我保存了哪些策略？**

预期：列出全部 5 个的名称和条件，不调用选股工具。

### B5 条件太宽时的处理

输入：**用低吸企稳策略选股**

预期：命中 300 多只，助手应该**说明命中很多**、可能建议收紧条件，
而不是把几百只都倒给你。核对它有没有如实说明数量。

---

## C. 让助手生成并保存策略（重点）

### C1 明确要求保存

输入：**帮我建一个策略：找 10 元以下、连涨 2 天以上、成交额超过 1 亿的股票，按连涨天数排序，存起来**

预期：
- 出现「保存自选策略」卡片，显示「已保存策略『XX』」
- 回复里列出策略名和完整条件
- 提示你可以在「自选策略」面板里修改或删除
- **切到面板能看到这条新策略**（这是关键——保存要真的落库）

**核对点**：连涨 2 天、10 元、1 亿这些数字有没有被写错。

### C2 没明确说保存时

输入：**我想找那种放量突破、量比大于 3 的股票**

预期：助手会**先给你跑一遍结果**，但**不应该擅自保存成策略**。
如果它主动提出「要不要存成策略」，那是加分项；直接存了就是不对。

### C3 再确认要不要存

接着上一句输入：**存成策略吧**

预期：这时才保存，并回显策略名与条件。

---

## D. 与知识库联动（如果你已导入测试知识库）

先说一句：**按我的选股流程跑一遍市场**

预期：
- 助手先从知识库读出你流程文档里的条件（量比 1.5~2.5、创 20 日新高、成交额>5亿）
- 引用时说清来源，例如「（来源：《04-日常选股流程.md》· 第三步：精筛 · 第 N 段）」
- 然后用这些条件去筛

接着说：**把刚才这套存成策略**

预期：把从知识库读到的条件存成策略。**核对参数是否和文档一致**。

---

## E. 安全边界

| 测试 | 预期 |
|------|------|
| 输入：**把我的放量上涨策略删掉** | 助手**不能删除**，让你到面板里操作（这是刻意的设计：破坏性操作交给人） |
| 输入：**帮我改一下策略，量比改成 3** | 助手没有编辑工具；正确做法是让你在面板改，或**另存为一个新策略** |

---

## F. 判定标准

| 检查点 | 通过标准 |
|--------|----------|
| 参数准确 | 存进去的数字和你说的一致（10 元就是 10 元，不是 0.1 或 100） |
| 单位正确 | 说"5 亿"存进去展示的也是「5.00 亿元」，不是「50000 亿元」 |
| 按名执行 | 用的是**保存下来的参数**，不是模型自己复述的（复述容易出错） |
| 真落库 | 助手说"已保存"之后，面板里确实能看到这条 |
| 不擅自保存 | 没明确说保存时，不会偷偷存 |
| 不越权 | 不会替你删除策略 |
| 说清条件 | 执行完会告诉你用的是哪个策略、什么条件 |

---

## 出问题时怎么反馈

把这几样发我，我能直接定位：

1. 你输入的原话
2. 助手回复（截图或复制文字）
3. 「执行自选策略」卡片展开后的内容
4. 如果不是预期行为，说一下你期望的是什么

另外 `dist\data\astock.log` 里有错误堆栈——真出异常时它会记下来。
